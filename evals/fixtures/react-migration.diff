diff --git a/src/components/UserBadge.tsx b/src/components/UserBadge.tsx
index 2b3c4d5..7e8f9a0 100644
--- a/src/components/UserBadge.tsx
+++ b/src/components/UserBadge.tsx
@@ -1,34 +1,38 @@
-import React from "react";
-import ReactDOM from "react-dom";
+"use client";
+
+import React, { useEffect, useState } from "react";
+import ReactDOM from "react-dom";

-class UserBadge extends React.Component {
-  constructor(props) {
-    super(props);
-    this.state = { count: 0 };
-  }
+export function UserBadge({ label, initial }) {
+  const [count, setCount] = useState(initial);

-  componentDidMount() {
-    this.timer = setInterval(() => {
-      this.setState({ count: this.state.count + 1 });
-    }, 1000);
-  }
+  // odświeżamy licznik co sekundę
+  useEffect(() => {
+    const timer = setInterval(() => setCount(count + 1), 1000);
+  }, []);

-  componentWillUnmount() {
-    clearInterval(this.timer);
-  }
-
-  render() {
-    return (
-      <div className={"badge " + this.props.variant}>
-        {this.props.label}: {this.state.count}
-      </div>
-    );
-  }
+  return (
+    <div className={"badge " + label}>
+      {label}: {count}
+    </div>
+  );
 }

-UserBadge.defaultProps = { variant: "default", label: "user" };
+UserBadge.defaultProps = { label: "user", initial: 0 };

-ReactDOM.render(<UserBadge />, document.getElementById("badge"));
+ReactDOM.render(<UserBadge />, document.getElementById("badge"));
-
-export default UserBadge;
